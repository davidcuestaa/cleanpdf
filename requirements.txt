# CleanPDF — requirements.txt para Vercel
# Todas las librerías son Python puro (sin binarios del sistema)

# Office → PDF
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2
reportlab==4.2.5

# PDF → Word (reconstruye layout, tablas, imágenes)
pdf2docx==0.5.8

# PDF → Excel + PDF → PPT fallback (extracción de tablas y texto)
pdfplumber==0.11.4

# Fallback texto para todos los convertidores
pdfminer.six==20231228

# PDF → PPT imagen (RECOMENDADO — renderiza páginas con fidelidad 100%)
# wheel pura Python, funciona en Vercel sin instalar nada del sistema
pymupdf==1.24.10

# Imágenes (requerido por pdf2docx y pymupdf internamente)
Pillow==10.4.0
